Sample Spring Boot Integration App.

Build the application using mvn clean install

The application can be run using jar command

access the application by the http://localhost:8080/upload

An ActiveMq instance can be started and the broker details can be specified to send the Orders.
